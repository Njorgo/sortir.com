{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouvelle ville | {{parent() }}{% endblock %}

{% block body %} <h2>Ajouter une nouvelle ville</h2>
{{ form_start(ajouterVilleForm) }}
{{ form_widget(ajouterVilleForm) }}
{{ form_end(ajouterVilleForm) }}
<div><a href="{{ path('admin_villes') }}" class="btn btn-primary">Annuler</a></div>
{% endblock %}
