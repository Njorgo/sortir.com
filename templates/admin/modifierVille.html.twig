{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau campus | {{parent() }}{% endblock %}

{% block body %} <h2>Ajouter un nouveau campus</h2>
{{ form_start(modifierVilleForm) }}
{{ form_widget(modifierVilleForm) }}
{{ form_end(modifierVilleForm) }}
<div><a href="{{ path('admin_villes') }}" class="btn btn-primary">Annuler</a></div>
{% endblock %}