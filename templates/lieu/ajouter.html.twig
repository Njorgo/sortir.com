{% extends 'base.html.twig' %}

{% block title %}Ajouter un lieu | {{parent() }}{% endblock %}

{% block body %}<h2>Ajouter un lieu</h2>
{{ form_start(creerLieuForm) }}

<div id="ajouter-lieu">
    <div>
        {{ form_row(creerLieuForm.nom) }}
        {{ form_row(creerLieuForm.rue) }}
        {{ form_row(creerLieuForm.latitude) }}
        {{ form_row(creerLieuForm.longitude) }}
        {{ form_row(creerLieuForm.ville) }}
        <div>{{ form_row(creerLieuForm.ajouter, {'attr':{'class':'btn btn-primary'}}) }}</div>
    </div>
            <div>
                <a href="{{ path('sortie_creer') }}" class="btn btn-primary">Annuler</a>
            </div>
        </div>
{{ form_end(creerLieuForm) }}
{% endblock %}